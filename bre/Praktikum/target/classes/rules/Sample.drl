package com.sample
 

import tools.Aircraft;
import dataobjects.*
 
rule "Test"
	when
		$a : Aircraft (wingSpan == 213.0)
		
		then
		System.out.println("rule erfolgreich"); 
end

rule "Test2"
	when
	$aixm : AixmMessage (messageId == "FNS_ID_c51e3152-af55-4af9-97af-b83a831f0daa")
	
	then
	System.out.println("rule erfolgreich AIXM");
 end
 
/*rule "Hello World"
    when
        m : Message( status == Message.HELLO, myMessage : message )
    then
        System.out.println( myMessage );
        m.setMessage( "Goodbye cruel world" );
        m.setStatus( Message.GOODBYE );
        update( m );
end

rule "GoodBye"
    when
        Message( status == Message.GOODBYE, myMessage : message )
    then
        System.out.println( myMessage );
end
*/
